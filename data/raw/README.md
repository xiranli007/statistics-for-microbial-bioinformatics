# Raw data

Raw datasets are not committed by default. A small source file may be included
when its reuse license explicitly permits redistribution and preserving it makes
the chapter reliably executable.

Every file in this directory must have a record in `data/datasets.yml` containing
its paper, source, license, checksum, and experimental-unit definition. Never
silently replace, manually edit, or fabricate a raw file.
